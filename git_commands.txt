git init (Initialized empty Git repository in...)

git status

git clone https://github.com/Hivernus/Formacao_HIQ_Java.git
(endereço retirado do rep. do github)

git add --all
git commit -m "inserir texto"

git config --global user.email "you@example.com"
git config --global user.name "Your Name"

git push
git pull

Comentar linha CTRL + 7(/)
-----------------
classe - definição de um tipo
Objecto - instância de uma classe

tipos de variáveis:
double - 64 bits
float - 32 bits
--------------
for(<instrução>;<condição boolean>;<instrução>)

p.ex: for(;;) - neste caso a condição inicializa sempre a true; for(;true;)

for (String arg : args) //foreach
	System.out.println(arg);
--------------
tipos primitivos em java:
short - 16 bits
int - 32 bits
float - 32 bits
double - 64 bits
char - 16 bits (hexadecimal)
long - 64 bits
byte - 8 bits
boolean - depende da situação

integrais - short, int, long, byte
virgula flutuante - float, double
----------------

System.out.println((10%2 == 0) ? "Par" : "Impar");

% - resto da divisão

-------------------
Operator Precedences:
1-parentheses
2-increment/decrement ++i, i++, --i, i-- 
3- * / %
4- + -
 
-------------------

o if é o resultado de uma expressao boolean

-------------------
Herança:

Uma classe (filho) herda métodos e atributos da classe (pai).
Não herda os construtores da classe.
(os construtores não são herdados)

extends (da super classe)
classe 1
	Classe 2
		Classe 3
			extends Class 2
			extends Class 1 ERRADO!

Polimorfismo:

ter uma referência de algo, mas essa referência poder "transforma-se"
em várias coisas.

JUnit - testes unitários
-------------------

Uma classe abstracta pode ter métodos implementados uma interface não



